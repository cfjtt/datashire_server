package com.eurlanda.datashire.sprint7.service.squidflow.subservice;

import com.eurlanda.datashire.adapter.IRelationalDataManager;
import com.eurlanda.datashire.sprint7.service.squidflow.AbstractRepositoryService;

/**
 * Extract SquidModelBase 业务处理 1.拖表创建，支持批量创建（从source table list选择一个或多个到面板空白区）
 * 2.拖列创建（从source table中选择一列或多列到面板空白区或空的extract上） 3.详细信息加载（query details when
 * load squid flow）
 * 
 * @author dang.lu 2013.11.12
 * 
 */
public class WebServiceExtractSquidServiceSub extends AbstractRepositoryService {

    public WebServiceExtractSquidServiceSub(String token) {
        super(token);
    }

    public WebServiceExtractSquidServiceSub(IRelationalDataManager adapter) {
        super(adapter);
    }

    public WebServiceExtractSquidServiceSub(String token, IRelationalDataManager adapter) {
        super(token, adapter);
    }

    

}